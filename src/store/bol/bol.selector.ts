import { IRootState } from "..";
import { IHeaderCellType } from "../../components/DynamicTable";

// export const getProcessorTableHeaderIdsMap = (
//     state: IRootState,
// ): string[] => state.bol.processingTableHeaders.reduce((acc: {} , h: IHeaderCellType) => {
//     acc[h.id] = h.id;
//     return acc;
// }, {});